# This file was automatically generated by workspacegen.py

{% for entry in content -%}
{% if entry.rule == "new_http_archive" -%}
{{ entry.rule }}(
    name = "{{ entry.name }}",
    build_file = "{{ entry.build_file }}",
    sha256 = "{{ entry.sha256 }}",
    strip_prefix = "{{ entry.strip_prefix }}",
    urls = [
    {% for url in entry.urls_list -%}
    "{{ url }}",
    {% endfor -%}
    ],
)
{% endif -%}
{% if entry.rule == "new_git_repository" -%}
!!!Work in progress!!!
{{ entry.rule }}(
    name = "{{ entry.name }}",
    build_file = "{{ entry.build_file }}",
    remote = "{{ entry.remote }}",
    sha256 = "{{ entry.sha256 }}",
    strip_prefix = "{{ entry.strip_prefix }}",
    urls = [
    {% for url in entry.urls_list -%}
    "{{ url }}",
    {% endfor -%}
    ],
)
{% endif -%}

{% endfor -%}