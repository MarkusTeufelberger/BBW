# This file was automatically generated by buildfilegen.py

{% for entry in header_list -%}
{{ entry }}
{% endfor -%}

{% for entry in content -%}
{% if entry.rule == "filegroup" -%}
{{ entry.rule }}(
    name = "{{ entry.name }}",
    srcs = [
    {% for src in entry.srcs_list -%}
    "{{ src }}",
    {% endfor -%}
    ],
)
{% endif -%}
{% if entry.rule == "pkg_tar" -%}
{{ entry.rule }}(
    name = "{{ entry.name }}",
    {% if entry.package_dir is defined -%}
    package_dir = "{{ entry.package_dir }}",
    {% endif -%}
    {% if entry.files_list is defined -%}
    files = [
    {% for file in entry.files_list -%}
    "{{ file }}",
    {% endfor -%}
    ],
    {% endif -%}
    {% if entry.deps_list is defined -%}
    deps = [
    {% for dep in entry.deps_list -%}
    "{{ dep }}",
    {% endfor -%}
    ],
    {% endif -%}
)
{% endif -%}

{% endfor -%}